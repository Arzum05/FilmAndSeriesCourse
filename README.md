# filmsAndSeriesBot

Простой Telegram‑бот на Python, который подбирает фильмы и сериалы через TMDb API. Бот может искать фильмы по названию, актёру, жанру, выдавать случайные рекомендации и находить трейлеры.

---

## 🚀 Возможности

* Подбор фильма по твоим предпочтениям (/start)
* Случайный популярный фильм (/random)
* Поиск фильмов по актёру (/actor_films)
* Поиск по жанру и году (/genres)
* Поиск фильма/сериала по названию (/search)
* Трейлеры фильмов (/trailer)
* Персональные рекомендации по предпочтениям (/recommend)

---

## 📦 Требования

* **Python 3.8+** (рекомендуется 3.10+)
* Библиотеки:

  ```bash
  pip install pytelegrambotapi requests python-dotenv
  ```

---

## 🔧 Настройка окружения

Создайте файл **.env** в папке с ботом:

```
TOKEN=ВАШ_TELEGRAM_BOT_TOKEN
TMDB_API=ВАШ_TMDB_API_KEY
```

* `TOKEN` — токен Telegram‑бота от BotFather.
* `TMDB_API` — ключ API с [https://www.themoviedb.org](https://www.themoviedb.org).

---

## ▶️ Запуск

```bash
python bot.py
```

После запуска бот будет работать в режиме polling.

---

## 📘 Основные команды

| Команда          | Описание                         |
| ---------------- | -------------------------------- |
| **/start**       | запуск опроса предпочтений       |
| **/help**        | список команд                    |
| **/search**      | поиск фильма/сериала по названию |
| **/random**      | случайный фильм                  |
| **/actor_films** | подборка фильмов с актёром       |
| **/genres**      | подбор по жанру и году           |
| **/trailer**     | поиск трейлера                   |
| **/recommend**   | рекомендации по предпочтениям    |

---

## 🧠 Как работает логика

* Все ответы пользователя сохраняются в `user_data[chat_id]`.
* После `/start` бот последовательно спрашивает жанр → актёра → тип (новый/старый) → фильм/сериал → топ/случайный.
* Команда `/recommend` формирует запрос через TMDb `/discover/movie` с учётом жанров, актёров и сортировок.
* Поиск актёров — `/search/person`.
* Получение топ‑5 фильмов актёра — `/person/{id}/movie_credits`.

---

## ⚠️ Возможные ошибки

### ➤ Error 409: terminated by other getUpdates request

Значит бот запущен в двух местах одновременно. Выключи второй процесс.

### ➤ TMDb возвращает пустой список

Проверь:

* год релиза
* жанр
* корректность `TMDB_API`

### ➤ Картинки не отправляются

Постер должен быть по шаблону:

```
https://image.tmdb.org/t/p/w500{poster_path}
```

---

## 🛠 Рекомендации по улучшению

* Хранить данные пользователей в SQLite, а не в памяти
* Добавить inline-кнопки вместо ReplyKeyboard
* Добавить обработку исключений запросов (`try/except`)
* Сделать красивый интерфейс с фотографиями и пагинацией
* Вынести логику TMDb в отдельный модуль

---

## 🔐 Безопасность

Никогда **не выкладывай .env** в публичный доступ.
Добавь `.env` в `.gitignore`.

---

## 📄 Лицензия

MIT License — можно использовать и изменять как угодно.

---

Если хочешь, могу сделать:

* GitHub‑версию с бейджами и форматированием
* Минимальный README или расширенный профессиональный
* Добавить установку через Docker

